# 1. OOM 테스트용 (정수, 50~512 범위, 단위: MB)
# 낮게 설정할수록 MemoryGuard에 의해 빨리 강제 종료됨
MEMORY_LIMIT=256

# 2. CPU 과점유 테스트용 (정수, 10~100 범위, 단위: %)
# 낮게 설정할수록 Watchdog에 의해 빨리 강제 종료됨
CPU_MAX_OCCUPY=80

# 3. 데드락(Deadlock) 테스트용 (true / false)
# true일 경우 스레드 경합으로 무응답 현상 발생
MULTI_THREAD_ENABLE=true